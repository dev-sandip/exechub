services:
  postgres:
    image: postgres:16
    container_name: exec-hub-postgres
    restart: "no"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres